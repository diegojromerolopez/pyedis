# pyedis documentation

pyedis exposes a Redis-compatible RESP2 TCP service. The architecture separates protocol framing, storage, command dispatch, persistence, and networking.

See [API reference](api.md).
