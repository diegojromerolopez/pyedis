# pyedis documentation

pyedis exposes a native RESP2 TCP API. The server is composed of a streaming protocol decoder, an asynchronous locked store, command dispatcher, and append-only persistence engine.

See [the API reference](api.md).
