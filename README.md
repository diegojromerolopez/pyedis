# pyedis

`pyedis` is an in-memory key-value store with a native Redis RESP2/RESP3 API over TCP.

## Usage

Run the server:
```bash
make run
```

Run tests:
```bash
make test
```

Run linter & type checker:
```bash
make lint
```
