[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pyedis"
version = "0.1.0"
description = "A small Python RESP2-compatible in-memory key/value server"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src"]
