[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pyedis"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src"]
