[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pyedis"
version = "0.1.0"
description = "In-memory Redis server implementation"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
