[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyedis"
version = "0.1.0"
description = "Native Redis RESP Key-Value Store in Python 3.14"
authors = [{ name = "Pyedis Maintainers" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "redis>=5.0.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "coverage>=7.6.0"
]

[tool.mypy]
strict = true
python_version = "3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
